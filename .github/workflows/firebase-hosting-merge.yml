# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install

      - name: Build project
        run: pnpm build
        env:
          VITE_API_BASE_URL: https://backend-zuiz.onrender.com
          VITE_MAPBOX_TOKEN: pk.eyJ1IjoiZW1pdGFhIiwiYSI6ImNtaXJoaXFmczA1eXUzZm9uaXoyenkyaG8ifQ.NTt1XYxjYiH_JtjsmWDfcA
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PETFINDER_REACT }}
          channelId: live
          projectId: petfinder-react
